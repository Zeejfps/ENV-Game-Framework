<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net9.0</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
        <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    </PropertyGroup>

    <ItemGroup>
      <!-- Abstractions only (IApp/IWindow/options). The neutral KeyboardKey enum is part of
           the IWindow surface; GLFW/OpenGL/Metal implementations live in ZGF.Core.Desktop. -->
      <ProjectReference Include="..\ZGF.KeyboardModule\ZGF.KeyboardModule.csproj" />
    </ItemGroup>

</Project>
