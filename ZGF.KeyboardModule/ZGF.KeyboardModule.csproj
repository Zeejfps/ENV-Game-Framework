<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <!-- iOS compile coverage is opt-in: `dotnet build -p:BuildIos=true` (needs the ios workload). -->
        <TargetFrameworks>net6.0$(IosTfmSuffix)</TargetFrameworks>
    </PropertyGroup>

</Project>
